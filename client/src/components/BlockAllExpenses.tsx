/**
 * This component is now empty - all expense blocking has been disabled
 * We keep the component in place in case we need to reinstate budget protection in the future
 */
const BlockAllExpenses = () => {
  // No budget protection - all expenses allowed
  return null;
};

export default BlockAllExpenses;